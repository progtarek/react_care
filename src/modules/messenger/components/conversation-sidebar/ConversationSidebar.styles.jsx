import styled from 'styled-components';
import { DESKTOP_SIDEBAR_WIDTH } from '../../../../shared/styles/variables';

export const ConversationSidebarContainer = styled.section`
  display: flex;
  height: 100vh;
  width: ${DESKTOP_SIDEBAR_WIDTH};
  background-color: red;
`;
